import React from 'react'
import ReactDOM from 'react-dom'

import ClientInfo from './ClientInfo'

const container = document.createElement('div')
document.body.appendChild(container)
ReactDOM.render(<ClientInfo />, container)
